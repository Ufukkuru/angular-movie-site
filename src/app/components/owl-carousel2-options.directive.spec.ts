import { OwlCarousel2OptionsDirective } from './owl-carousel2-options.directive';

describe('OwlCarousel2OptionsDirective', () => {
  it('should create an instance', () => {
    const directive = new OwlCarousel2OptionsDirective();
    expect(directive).toBeTruthy();
  });
});
